# Severity rubric -- dashboard domain

> **Not for single-pass reviews.** This file is one domain's slice of
> `../../severity-rubric.md`, written for a reviewer that owns only this
> domain within a larger, domain-split review. If you are running the
> `review-integration` skill end to end, read the monolith instead -- it
> carries every domain's rows and is the authoritative copy.

Severity definitions and the new-vs-existing calibration note live in
`../severity-core.md`. Read that file first. Conflicts for this domain:
core only (`../conflicts-core.md`); there is no dashboard-specific
conflict file.

`../../severity-rubric.md` carries no dashboard rows today. This domain's
severity calibration currently lives in the `dashboard-review` skill (see
its `references/review-procedure.md`) and the `dashboard-guidelines`
skill. Domain rows land in this file when they exist in the monolith — do
not invent them here.
